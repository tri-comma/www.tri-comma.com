version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - git lfs install
        - git lfs pull
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
